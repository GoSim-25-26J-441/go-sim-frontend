# -----------------------------
# Backend (server-side only)
# -----------------------------

# Base URL of your backend gateway/server.
# This is used by Next.js API routes to call the backend (NOT exposed to the browser).
BACKEND_BASE=http://127.0.0.1:8000

# Secret key required by the Design Input backend API (server-side only).
# Put the real value in your local .env (never commit the real key).
DESIGN_INPUT_API_KEY=


# -----------------------------
# App (client-safe / public)
# -----------------------------

# App name shown in UI (safe to expose to browser).
NEXT_PUBLIC_APP_NAME=GO-SIM


# -----------------------------
# Firebase (client-safe / public)
# -----------------------------
# These are Firebase web config values (safe to expose to the browser).
# Fill them from Firebase Console -> Project settings -> Your apps (Web app).

# Firebase Web API key
NEXT_PUBLIC_FIREBASE_API_KEY=

# Firebase Auth domain
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=

# Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID=

# Firebase Storage Bucket (usually: your-project-id.appspot.com)
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=

# Firebase Messaging Sender ID
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=

# Firebase App ID
NEXT_PUBLIC_FIREBASE_APP_ID=
